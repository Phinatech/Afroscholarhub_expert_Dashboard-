// import React from 'react'

import { Outlet } from "react-router-dom"

const VerifyRedirectLayout = () => {
  return (
    <div>
        <Outlet />
    </div>
  )
}

export default VerifyRedirectLayout